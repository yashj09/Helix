node_modules/
build/
cache/
artifacts/
typechain-types/
.env
.env.local
*.log
.DS_Store
dist/
deployments/localhost/

# Oracle's persistent key cache (encrypted, but still sensitive)
helix-oracle-keys.enc
*.keys.enc
